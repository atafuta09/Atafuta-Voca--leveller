# Atafuta09Leveler - 取扱説明書 (User Manual)

**Atafuta09Leveler** は、JUCE / C++ で開発されたプロ仕様のボーカル特化型オートレベラー（Vocal Dynamics Rider）VST3 プラグインです。  
コンプレッサーとは異なり、原音のダイナミクスや質感を損なうことなく、ボーカルの手動ボリュームオートメーション（手書きフェーダー操作）を自動化し、オケに埋もれない安定したボーカルトラックを実現します。

---

## 画面構成・インターフェース解説

プラグイン画面は大きく分けて3つのエリアで構成されています：
1. **トップヘッダーバー**（ロゴ、バージョン、リンク、カラー切替、プリセット、ズーム、フィルター、バイパス）
2. **左側 CONTROL PANEL**（音量制御・検出・タイミング設定）
3. **中央 WAVEFORM DISPLAY & 右端 METERS**（リアルタイム波形および入出力メーター）

---

## 1. トップヘッダーバー

### ロゴ ＆ バージョン情報
- **Atafuta09Leveler / Ver 1.04**
  - プラグイン名と現在のバージョンが表示されます。

### 公式リンクボタン
- **`X`**: 公式 X（Twitter: [@atafuta09](https://x.com/atafuta09)）をブラウザで開きます。
- **`YT`**: 公式 YouTube チャンネル（[@atafuta09](https://www.youtube.com/@atafuta09)）をブラウザで開きます。

### カラーテーマ切替 (`COLOR`)
- **`DARK` / `WHITE`**:
  - クリックするたびに **Dark Mode**（実機梨地ブラック＆サイバーブルー）と **White Mode**（高品位スタジオシルバー＆ホワイト）がワンタッチで切り替わります。
  - ホワイトモード時も高コントラスト設計により、すべての文字・ライン・目盛りがクッキリ視認できます。

### プリセットセレクター ＆ 保存機能
- **プリセットドロップダウン**:
  - 目的のボーカルスタイルに合わせたプリセットを瞬時に呼び出せます。
- **`SAVE` ボタン**:
  - 現在設定しているパラメータをお好みの名前で保存できます。
  - 保存されたプリセットはドロップダウンに即座に追加され、PC内のユーザー設定領域（`%APPDATA%`）に自動永続化されます。次回DAW起動時にも消えずに復元されます。

### UIスケーリング (`-` / `100%` / `+`)
- **拡大・縮小コントロール**:
  - `[-]` で10%縮小、`[+]` で10%拡大できます（最小50% 〜 最大130%）。
  - ノートPCや4K高解像度モニターなど、画面環境に合わせて最適なサイズに調整可能です。

### プロセッシングフィルター・バイパス
- **`BREATH` (ブレスフィルター)**:
  - ONにすると、150Hz以下の吹かれノイズやマイクへの息の吹き込みをカットし、ブレス（息継ぎ）区間でゲインが不自然に持ち上がってしまう現象を防ぎます。
- **`SIBILANCE` (歯擦音フィルター)**:
  - ONにすると、4kHz以上の「サ行」「タ行」の鋭い子音ノイズによる誤検出を抑制し、過剰な音量減衰（ダッキング）を防ぎます。
- **`BYPASS` (バイパス)**:
  - プラグインの音量補正処理を一時的にスルーし、原音と処理後の音量を瞬時に比較試聴できます。

---

## 2. 左側 CONTROL PANEL（主要操作パラメータ）

### SPEED ノブ（アタック・リリース制御）
- **役割**: ボーカルの音量変化にどれくらい素早くゲインを追従させるかを設定します。
- **Free モード時**:
  - 0%（緩やか）〜 100%（俊敏）で無段階調整。
  - ノブ下部のバッジに、計算された **Attack（アタック時定数）** と **Release（リリース時定数）** がリアルタイム表示されます。
- **Sync モード時**:
  - ホストDAWのBPMテンポに同期し、**Slow / Mid / Fast** の3段階ステップ動作に自動切り替え。
  - ビートの揺れに合わせて音楽的に追従します。

### RANGE ノブ（最大補正幅）
- **設定範囲**: 0.0 dB 〜 15.0 dB
- **役割**: ゲインを持ち上げる（または下げる）最大許容範囲を設定します。
- **使い方のコツ**:
  - バラードなどで自然に整えたい場合は `4.0 dB 〜 6.0 dB` 程度。
  - 激しいロックやポップスで均一に前に出したい場合は `8.0 dB 〜 12.0 dB` 程度が推奨です。

### IN GAIN / OUT GAIN スライダー
- **IN GAIN**: レベラー回路に入る前の入力音量をブースト／カット（±15 dB）します。
- **OUT GAIN**: 音量均一化処理後の最終出力音量を調整（±15 dB）します。

### DETECTOR セレクター（音量検出方式）
- **RMS (推奨)**: 人間の耳の音量感（平均エネルギー）に近い滑らかで自然な追従を行います。通常のボーカルに最適です。
- **Peak**: 突発的なピーク音量にも瞬時に反応します。アタックの強いラップやパーカッシブなボーカルに効果的です。

### TIMING セレクター
- **Free (ms)**: ミリ秒単位での連続的なレスポンス制御。
- **Sync (BPM)**: 楽曲テンポと同期したステップ制御。

### TARGET LEVEL フェーダー (Nectar 4 方式メーター統合)
- **設定範囲**: -36.0 dBFS 〜 0.0 dBFS
- **役割**: 目標とするボーカル音量レベルを設定します。
- **内部メーター表示**: フェーダーの溝内部に、現在の入力信号がリアルタイムに青白く発光して光り上がります。メーターの山がフェーダーのつまみ位置に来るように設定することで、直感的に目標レベルを合わせられます。

### 下部トグル
- **`LOOKAHEAD (5ms)`**:
  - 5msの先読みバッファを有効化します（DAWの自動遅延補正に対応）。
  - 急な大声や突発的な叫び声に対しても、アタックの頭潰れを起こさず完璧に制御できます。
- **`GUI RENDER`**:
  - 中央波形の描画を一時停止／再開します。描画をOFFにしても内部の音量補正DSPはフルパワーで動作し続けます。

---

## 3. 中央 WAVEFORM DISPLAY & 右端 METERS

### 中央ディスプレイ（リアルタイム描画）
- **入力波形 (Input Wave)**: 白熱グレーのゴースト波形として原音を表示。
- **出力波形 (Output Wave)**: エレクトリックシアンの鮮やかな波形として補正後の音量を表示。
- **ゲイン補正線 (GR Ride)**: 中央のターゲットラインを基準に、上下にリアルタイムに走るネオンピンクのレーザー軌跡で、プラグインがどれだけゲインを稼いでいるか／抑えているかを視覚化。
- **ターゲットライン & レンジ帯**: ターゲットフェーダーとレンジノブの操作に追従して表示領域がリアルタイムに連動。

### 右端スリムメーター
上部のドロップダウンでメーター動作を切り替え可能：
- **Peak**: 瞬時ピーク値を表示。約1.5秒保持される**ピークホールドライン**付き。
- **RMS**: 平均音量レベルを表示。
- **VU-18**: 業務用音響基準（0 VU = -18 dBFS）で針のように動作。0 VU を超えると赤く点灯。

---

## 4. プリセット一覧・推奨用途

| プリセット名 | 推奨用途・特徴 |
| :--- | :--- |
| **Default** | 標準的なポップスボーカル向け。迷ったらまずこれを選択。 |
| **Vocaloid** | 初音ミク等のボーカロイドや打ち込み系ボーカル特化。俊敏な追従とフィルターを全適用。 |
| **Gentle Vocal Ride** | アコースティックやジャズなど、抑揚を残して優しく均一化したい場合。 |
| **Aggressive Leveler** | ロックやメタル、激しいアニソンなど、常に音量を前に張り付かせたい場合。 |
| **Fast Peak Tamer** | ピーク検出を用い、突発的な叫び声やアクセントを素早く抑え込む設定。 |
| **BPM Sync 1/4 (Mid)** | 楽曲の4分音符ビートに同期して音楽的に波打つレベリング。 |
| **Podcast / Voiceover** | YouTube解説動画、ナレーション、ポッドキャストなどのトーク音声を明瞭化。 |
| **Ballad Vocal** | スローテンポのバラード向け。ロングトーンを美しく保つ長めのリリース。 |
